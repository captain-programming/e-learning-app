import { StyleSheet } from "react-native";

export const Style = StyleSheet.create({
  main: {
    flex: 1,
    padding: 30,
    backgroundColor: "white"
  },
  buttonBox: {
    alignItems: "flex-end"
  }
})